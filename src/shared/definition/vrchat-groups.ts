export interface GroupSummary {
  groupId: string
  groupName: string
  shortCode: string
  description: string
  iconFileId: string
  iconFileVersion: number
  bannerFileId: string
  bannerFileVersion: number
  ownerUserId: string
  isVerified: boolean
}
